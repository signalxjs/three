/**
 * @sigx/runtime-three — the three.js renderer for SignalX.
 *
 * Scaffold entry. The renderer (host node ops, frame loop, pointer events,
 * JSX intrinsics) lands in the follow-up PRs tracked from
 * https://github.com/signalxjs/three/issues/1.
 */
export {};
